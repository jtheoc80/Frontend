"import React, { useState } from 'react';\nimport axios from 'axios';\n\nconst TwoFactorAuth: React.FC = () => {\n    const [code, setCode] = useState('');\n    const [message, setMessage] = useState('');\n\n    const handleSubmit = async (event: React.FormEvent) => {\n        event.preventDefault();\n\n        try {\n            // Send POST request to backend to verify 2FA code\n            const response = await axios.post('/api/verify_2fa', { code });\n            if (response.data.success) {\n                setMessage('2FA verified successfully.');\n            } else {\n                setMessage('Invalid 2FA code. Please try again.');\n            }\n        } catch (error) {\n            console.error('Error verifying 2FA code:', error);\n            setMessage('Error verifying 2FA code. Please try aga..." 
